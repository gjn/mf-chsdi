window.GeoAdmin = {
    version: "%API_VERSION%",
    webServicesUrl: "http://map.geo.admin.ch"

    // FIXME: should be replaced by:
    //webServicesUrl: "%HOST%%WSGI_BASE%/ws"
};

// the tests, and possibly other stuff, don't load Ext, so
// do not initialize the Ext QuickTips if Ext isn't there.
if (typeof Ext !== "undefined") {
    Ext.QuickTips.init();
}

OpenLayers.ImgPath = "%HOST%%WSGI_BASE%/%API_VERSION%GeoAdmin.ux/Map/img/";
